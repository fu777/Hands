<% if @checks.exists? %>
  <%= render partial: 'check', collection: @checks %>
<% else %>
  <div class="container">
    <div class="row col-12 d-flex justify-content-center mt-5">
      <p>やることはありません。</p>
    </div>
  </div>
<% end %>
