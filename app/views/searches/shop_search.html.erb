<%= render 'shop_search', shop: @shop %>

<% if @shops.present? %>
  <% @shops.each do |shop| %>
    <%= image_tag shop.get_shop_icon_image(100,100) %><br>
    <%= link_to shop_path(shop.id) do %>
    <%= shop.name %><br>
    <% end %><br>
  <% end %>
<% else %>
  <p>ショップの検索結果はありませんでした。</p>
  <p>キーワードを変更してもう一度検索してください。</p>
<% end %>
  